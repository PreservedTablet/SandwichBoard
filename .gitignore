# Operator-local files — never committed (see docs/plan/04-INTEGRATIONS.md)
.infisical.json
.env
.env.*
!.env.example

# Dependencies & build output
node_modules/
dist/
build/
.svelte-kit/
*.tsbuildinfo

# Local storage adapter root (operator data, never committed)
data/

# /analyze output — real spend data, operator-local by design (Phase 3)
reports/

# Test & tooling output
coverage/

# Local tool state
.claude/settings.local.json

# OS noise
.DS_Store
Thumbs.db
